<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/jokesFragment">

    <fragment
        android:id="@+id/jokesFragment"
        android:name="space.rodionov.chucknorris.ui.jokes.JokesFragment"
        android:label="@string/jokes"
        tools:layout="@layout/fragment_jokes" />
    <fragment
        android:id="@+id/webFragment"
        android:name="space.rodionov.chucknorris.ui.web.WebFragment"
        android:label="@string/web"
        tools:layout="@layout/fragment_web" />
</navigation>